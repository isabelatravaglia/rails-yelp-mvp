<h1>Restaurants</h1>
<h3><%= link_to 'Add a new restaurant', new_restaurant_path %> </h3>
<ul>
  <% @restaurants.each do |r| %>
    <li> <%= link_to r.name, r %> - <%= r.category %> - <%= r.phone_number %> </li>
  <% end %>
</ul>
